apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-detection-chart.fullname" . }}
  labels:
    {{- include "flask-detection-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-detection-chart.selectorLabels" . | nindent 4 }}
