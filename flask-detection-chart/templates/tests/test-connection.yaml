apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flask-detection-chart.fullname" . }}-test-connection"
  labels:
    {{- include "flask-detection-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flask-detection-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
